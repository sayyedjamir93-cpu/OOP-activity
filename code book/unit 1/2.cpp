#include <iostream>
using namespace std;
int main()
{
    int marks = 45;  // marks stores student score.
    if (marks >= 40) // if checks pass condition.
    {
        cout << "Pass";
    }
    else // else executes when condition is false.
    {
        cout << "Fail"; // cout prints result.
    }
    return 0;
}